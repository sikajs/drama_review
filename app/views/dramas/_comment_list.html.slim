.review
  = simple_format(comment.content)
  .star-rating data-score== comment.rating
  p
    = t('by')
    span.show_user
      = "#{comment.user.name} "
    = "#{time_ago_in_words(comment.created_at)} #{t('ago')}"
hr
